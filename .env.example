# Nuuk Bus Live Map Configuration

# Realtime feed org_id (default: 968 for Nuuk)
# To change the feed source, update this value
VITE_ORG_ID=968

# Polling interval in milliseconds (default: 8000 = 8 seconds)
VITE_POLL_MS=8000

# =============================================================================
# MAP TILES CONFIGURATION
# =============================================================================
# The app supports two tile providers:
# 1. Maptiler (vector tiles) - Higher quality, Uber/Bolt-like appearance
# 2. Raster tiles (CARTO, OSM, etc.) - Fallback if no Maptiler key

# -----------------------------------------------------------------------------
# Option 1: Maptiler (Recommended)
# -----------------------------------------------------------------------------
# Get a free API key at https://cloud.maptiler.com/
# Free tier: 100K requests/month (enough for ~50-100 daily users)
#
# VITE_MAPTILER_API_KEY=your_api_key_here
#
# Style options (see https://cloud.maptiler.com/maps/):
# - streets-v2 (default light) - Clean street map
# - streets-v2-dark (default dark) - Dark version
# - basic-v2 / basic-v2-dark - Simpler, faster loading
# - toner-v2 / toner-v2-dark - High contrast B&W
# - dataviz / dataviz-dark - Optimized for data overlays
# - outdoor-v2 - Topographic/hiking style
#
VITE_MAPTILER_API_KEY=
VITE_MAPTILER_LIGHT_STYLE=streets-v2
VITE_MAPTILER_DARK_STYLE=streets-v2-dark

# -----------------------------------------------------------------------------
# Option 2: Raster tiles (Fallback)
# -----------------------------------------------------------------------------
# Used when VITE_MAPTILER_API_KEY is not set
# Supports {s} for subdomains and {r} for retina (@2x) tiles
#
VITE_TILE_LIGHT_URL=https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png
VITE_TILE_DARK_URL=https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png
VITE_TILE_ATTRIBUTION=&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/attributions">CARTO</a>
VITE_TILE_SUBDOMAINS=abcd
VITE_TILE_MAX_ZOOM=19

# =============================================================================
# ROUTE OVERLAYS
# =============================================================================
VITE_ENABLE_ROUTES_LAYER=false
VITE_ROUTE_OUTLINE_WIDTH=0

# Feature flags
VITE_ENABLE_STATIC_LAYERS=false

# =============================================================================
# OSRM ROUTING (snapped paths)
# =============================================================================
VITE_OSRM_BASE_URL=https://router.project-osrm.org
VITE_OSRM_PROFILE=driving

# =============================================================================
# API CONFIGURATION
# =============================================================================
# Production API base URL (leave empty for dev proxy)
# Set this to your Cloudflare Worker URL in production
# Example: https://nuuk-bus-api.your-subdomain.workers.dev
VITE_API_BASE_URL=
